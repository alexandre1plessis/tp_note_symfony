{% extends 'base.html.twig' %}
{% block body %}
<script>
    function addToCart() {
        fetch("/cart/add/{{ product.id }}")
            .then(data => {
            document.getElementById('btnpanier').disabled = true;
        })
    }
</script>
    <h2>{{ product.name }}</h2>
    <p>{{ product.description }}</p>
    <p>Date : {{ product.createdAt|date('d/m/Y') }} Prix : {{ product.price }}€</p>
    <button id="btnpanier" onclick="addToCart()">Ajouter au panier</button>
{% endblock %}




